{
  "name": "fintrackly-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.2",
    "razorpay": "^2.9.5"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
